# Follow-ups from the orchestrated issue run

Collected from implementing-agent reports on issues #32–#42, landed on the shared
feature branch `claude/kotlin-notion-orchestrate-9ztk5a`. Every entry is something
an agent **noticed and deliberately did not fix** because it fell outside the scope
of its issue. Nothing here has been independently verified — each item is recorded
as the agent reported it.

Status: `open` · `filed` (issue created) · `done` · `wont-do`

## Filed issues (triage of 2026-08-22)

All open items were grouped into seven issues, each sized for one independent agent
(except #62, which is maintainer decisions). Every issue works from and PRs back into
`claude/kotlin-notion-orchestrate-9ztk5a`, and must update this file on completion.

| Issue | Scope | Items | Recommended model |
|---|---|---|---|
| [#56](https://github.com/jsaabel/kotlin-notion-client/issues/56) | Build: `integrationTest`/`testAll` tasks + DataSource fixture decode fix | 9, 15 | sonnet |
| [#57](https://github.com/jsaabel/kotlin-notion-client/issues/57) | Rollup `incomplete` variant + forward-compat fallbacks | 12, 13 | sonnet |
| [#58](https://github.com/jsaabel/kotlin-notion-client/issues/58) | Small-surface polish batch (529 exception, error docs, SearchFilter default, filter_properties) | 1, 2, 4, 5, 6, 7, 8 | sonnet |
| [#59](https://github.com/jsaabel/kotlin-notion-client/issues/59) | Sibling-surface features: markdown page create, ViewsApi drain, typed rollup config | 18, 25, 33, 36 | opus |
| [#60](https://github.com/jsaabel/kotlin-notion-client/issues/60) | Vendored API reference refresh + link sweep | 3, 17, 27, 32, 35 | sonnet |
| [#61](https://github.com/jsaabel/kotlin-notion-client/issues/61) | Live-API verification pass (**depends on #56**) | 10, 19, 20, 22, 23, 26, 34, 38 | fable |
| [#62](https://github.com/jsaabel/kotlin-notion-client/issues/62) | Design ratifications & release checklist | 21, 24, 28, 29, 30, 37 | n/a (human) |

Not filed: 11 and 14 were already resolved during the run; 16 and 31 are design
cautions with no current action (`wont-do` with notes below).

Suggested order: #56 → #61 last (it adjudicates the others' claims); #57, #58, #59,
#60 are independent and can run in parallel; #62 whenever the maintainer has time.

| # | From | Follow-up | Status |
|---|------|-----------|--------|
| 1 | #32 | `docs/error-handling.md` (~lines 300–345) documents a `RateLimitConfig` API that no longer exists (`strategy`, `baseDelayMs`, `maxDelayMs`, `respectRetryAfter`, and the CONSERVATIVE/BALANCED/AGGRESSIVE presets), and never states which statuses are retried. Already stale before this run. Resync it and document the 429/529/502/503/504 retry matrix. | done — rewrote the Rate Limiting section against the actual `RateLimitConfig`/`NotionRateLimit` shape and added the 429/529/502/503/504 retry matrix, cross-checked against `NotionRateLimit.kt` (429/529 retry via `Retry-After` or backoff; 502/503/504 exponential backoff; 500 and other 4xx not retried) |
| 2 | #32 | No typed exception for service overload — a `529` that exhausts retries surfaces as a generic `NotionException.ApiError` with `status = 529`. A dedicated subtype, or one carrying `retryAfterSeconds`, would be an improvement. | done — added `NotionException.ServiceOverloadedError(retryAfterSeconds, details)`; also centralized the previously-duplicated per-API-class error-mapping boilerplate into a shared `HttpResponse.toNotionApiError()` so the new type actually reaches every endpoint, not just one |
| 3 | #33 | Vendored API reference `reference/notion-api/` is behind the live changelog — nothing newer than `documentation/general/12_Upgrade_Guide_2026-03-11.md`. Agents are web-fetching the live changelog to confirm field shapes. Refreshing the vendored docs would remove that dependency. **Corroborated by #34** (`filter_properties` appears nowhere — not in `NotionAPI.yml`, not in the sample responses) **and #35** (zero mention of `unknown_block` anything; the whole truncation-metadata feature is absent). | filed (#60) |
| 4 | #33 | `SearchFilter.property` still defaults to `"object"` in the primary constructor, so a hand-written `SearchFilter(inTrash = true)` that bypasses the builder emits a spurious `"property":"object"` alongside `in_trash`. Needs a decision: flip the default to `null` (behaviour change for direct constructor callers) or add `init` validation. | done — flipped the default to `null` (a documented, `CHANGELOG`-recorded breaking change for direct-constructor callers) and added `init` validation rejecting `property` set without `value`, or set to anything but `"object"` |
| 5 | #33 | No `is_archived` convenience overload on `DataSourcesApi.query`/`queryAsFlow`/`queryFirstPage`, and no trash variant of `SearchApi.search(query: String)`. Both are reachable via the request/builder objects; left alone for minimal scope, but worth filing if the ergonomics bite. | done — added `query`/`queryAsFlow`/`queryFirstPage(dataSourceId, isArchived: Boolean)` overloads and `SearchApi.search(query: String, inTrash: Boolean)` |
| 6 | #34 | IDEAS.md #5 is **not** closed by #34. The percent-decode fix lives in `PagesApi` only (`decodePropertyId`); normalising property IDs during `DataSource` schema deserialisation — what #5 actually asks for — is untouched. If #5 is picked up, consider promoting `decodePropertyId` into a shared util, which would make the `PagesApi` helper redundant. | done — `DatabasePropertySerializer` now decodes `id` before dispatching to each `DatabaseProperty` variant; promoted the helper to `utils.PropertyIds.decode` and removed the `PagesApi`-local copy, which now reuses it via the new `FilterPropertiesSupport.kt` |
| 7 | #34 | `filter_properties` is documented as capped at 100 IDs; no validation was added, since `RequestValidator` is body-shaped and this is a query-param concern. Add fail-fast validation if wanted. | done — added `validateFilterPropertiesLimit()`, called at each call site (`PagesApi.retrieve/create/update`, `DataSourcesApi.query/queryAsFlow/queryFirstPage`) before any HTTP call, so the `IllegalArgumentException` isn't folded into `NetworkError` by the surrounding try/catch |
| 8 | #34 | The same `filter_properties` parameter exists on data source query (`POST /v1/data_sources/{id}/query`), where the payload saving is larger still because we auto-paginate there. Out of scope for #34 — worth filing separately. | done — `filterProperties: List<String>?` threaded through `DataSourcesApi.query`/`queryAsFlow`/`queryFirstPage`, same repeated-query-param shape and 100-ID cap as `PagesApi`; confirmed the param name and cap against live docs (no `[]` suffix, `maxItems: 100`) |
| 9 | #35 | **CLAUDE.md documents build tasks that do not exist.** `./gradlew integrationTest` and `./gradlew testAll` are not configured — `build.gradle.kts` has only `tasks.test { useJUnitPlatform() }`, with no tag-based task split. Either add the tasks or fix the doc. Agents are working around it with `-Dkotest.tags.include="Unit"`. | done — `build.gradle.kts` now has `test` (excludes `RequiresApi` via `kotest.tags.exclude`), `integrationTest` (includes `RequiresApi`), and `testAll` (depends on both); CLAUDE.md already documented this exact shape, so no doc changes were needed |
| 10 | #35 | The REST reference page for page-markdown (`developers.notion.com/reference/get-page-markdown`) 404s, and the Aug 7 2026 changelog frames truncation metadata as an MCP `notion-fetch` change. The REST shape of `unknown_block_count` is therefore inferred, not documented — worth confirming against a live response. | filed (#61) |
| 11 | #35 | `CHANGELOG.md` has not been touched by any PR on this branch (#45–#48). Decision pending: batch one Unreleased → Added entry covering the whole run, or have each agent write its own. | done — batched `## [Unreleased]` → "August 2026 API catch-up" entry landed in 040aa50 |
| 12 | #36 | **Same crash class still live:** Notion documents rollup value types as `array \| date \| incomplete \| number \| unsupported`. `RollupResult` has no `incomplete` variant, so `"type": "incomplete"` still throws and fails deserialization of the whole page. Needs its own issue. | done — added `RollupResult.IncompleteResult` (mirrors `UnsupportedResult`'s shape); no official sample covers it, so the test fixture is hand-crafted from the documented `array \| date \| incomplete \| number \| unsupported` shape |
| 13 | #36 | **Forward-compat hardening:** `FormulaResult` and `RollupResult` have no catch-all fallback at all, unlike `PageProperty` with its `Unknown`. Any future nested type Notion adds hard-fails page deserialization. A `defaultDeserializer` on those two sealed classes would close the class of bug rather than one instance. | done — both sealed classes now carry a hand-rolled `KSerializer` (`FormulaResultSerializer`/`RollupResultSerializer`, same pattern as `PagePropertySerializer`) with an `Unknown(type, rawContent)` default branch; found no built-in kotlinx module-based default-deserializer wiring in this codebase to hook into, so the fallback lives in the discriminator dispatch itself, same as `PageProperty.Unknown` |
| 14 | #36 | **Conflicting testimony on test scoping — verify.** The #36 agent reports plain `./gradlew test` has no tag filter in `build.gradle.kts` and would run integration specs (it scoped with `--tests "unit.*"`). Agents on #32/#33/#34/#35 ran plain `./gradlew test` and reported the `RequiresApi` specs skipped. **Settled by #37:** `build.gradle.kts` has no tag filtering, so a bare `./gradlew test` does load `integration.*` specs — they are guarded by their own `@Tags("Integration","RequiresApi")` and env-var checks, not by the build script. With `-Dkotest.tags.include="Unit"` they load but skip; with `--tests "unit.*"` they are not loaded at all. `NOTION_API_TOKEN` is unset in the agent environment, so no run reached the live API. Item 9 (the missing tasks) still stands. | done |
| 15 | #37 | **Pre-existing fixture/model mismatch:** `DataSource.description` is a required non-nullable `List<RichText>`, but the official sample `src/test/resources/api/data_sources/get_retrieve_a_data_source.json` has no `description` key — so `TestFixtures.DataSources.retrieveDataSource().decode<DataSource>()` throws `MissingFieldException`. No existing test decodes that fixture into `DataSource`, which is why it went unnoticed. Either default `description` to `emptyList()` or refresh the fixture. | done — defaulted `DataSource.description` to `emptyList()`; confirmed via the live API reference that `description` is genuinely absent from the `partialDataSourceObjectResponse` variant, so the fixture reflects a real response shape rather than a stale sample; added a unit test in `DataSourcesApiTest` decoding the fixture |
| 16 | #37 | If a mention-aware link resolver lands (deferred to v0.6.0+ per `RichTextHtml.kt` KDoc), that resolver is where a `notion.so` domain assumption could creep back in. Nothing depends on mention `href` shape today — `RichTextHtml` drops them. | wont-do — caution for future v0.6.0+ link-resolver work, already recorded in `RichTextHtml` KDoc; nothing actionable now |
| 17 | #37 | `notebooks/README.md` and `QUICKSTART.md` link to `https://www.notion.so/my-integrations` — a settings page, not a record link, so out of scope for #37. Worth a sweep to `notion.com` eventually. | filed (#60) |
| 18 | #38 | The Jun 29 2026 changelog also enables `allow_async` on `POST /v1/pages` when a `markdown` body is supplied. `PagesApi.create` has no markdown path at all, so that half of the feature has no surface in this client. Worth a follow-up if page-create-from-markdown lands. | done — `CreatePageRequest.markdown` already existed; the missing surface was a markdown-shaped call plus `allow_async`. Added `createFromMarkdown`/`createFromMarkdownAsync`/`createAsync` returning a sealed `AsyncPageCreateResult`. |
| 19 | #38 | No integration test covers the async markdown path: a 202 cannot be forced deterministically (the API decides), so a live test would be flaky. The `Completed` fallback makes the code safe either way, but an opportunistic integration exercise could be filed. | filed (#61) |
| 20 | #38 | Async-task field optionality is modelled defensively (`operation`, `created_time` nullable) at **medium** confidence — endpoint path, five status values, `allow_async` and the 202 flow are high-confidence, cross-checked against Notion's MCP tool schema. Worth confirming minor-field optionality against a real 202 response. | filed (#61) |
| 21 | #38 | `pollAsFlow` deliberately does not throw on a failed task (it completes at the first terminal status), unlike `waitForCompletion` which throws `AsyncTaskException.TaskFailed`. Intentional, but the asymmetry is worth a conscious ratification. | filed (#62) |
| 22 | #39 | **IDEAS.md #4 is not unblocked**, contrary to the "bonus" note on #39. `NewFilterTypesIntegrationTest` also needs a `unique_id` property, still absent from the live property-schema reference's creatable types and from `CreateDatabaseProperty`. The status side of that setup is now fully API-doable. Either annotate #4 or file a narrower issue to make the test self-contained minus the unique_id filters. | filed (#61) |
| 23 | #39 | Status group assignment is implemented at high confidence from live docs but **never exercised against the live API** — integration tests were out of bounds for this run. The existing "verify standard groups" test in `DatabaseFeaturesIntegrationTest` is the natural place for a live assertion. | filed (#61) |
| 24 | #39 | Documented API behaviour worth ratifying: on update, an omitted `group` preserves the option's current group, and new options default to "To-do". Currently captured only in KDoc. | filed (#62) |
| 25 | #40 | **`ViewsApi` has the same 10,000-row cap with no windowed drain.** `request_status` exists on `ViewQuery`/`ViewQueryResults`, but the new `iterateAllRows` engine covers data sources only. Likely deserves its own issue. | done — views **cannot** be windowed: the query endpoint takes only `page_size` and a cached query takes no filter. Per Notion's own guidance `views.iterateAllRows` drains the view's data source with the view's filter; view sorts/quick_filters do not carry over (documented in KDoc). |
| 26 | #40 | The windowing key choice is **the agent's design, not a documented one** — Notion's "Query large data sources" guide (linked from the changelog) 404s at every obvious URL. The assumption that a `unique_id` property supports property sorts is only fairly confident. A one-off live drain against a >10k-row data source would settle both. | filed (#61) |
| 27 | #40 | The JS SDK named in #40 as prior art (`iterateAllDataSourceRows`/`collectAllDataSourceRows`) is **not vendored** under `reference/` — only `python/` and `notion-api/` are there. CLAUDE.md points agents at `reference/` implementations for cross-checking; worth vendoring the JS SDK or correcting that expectation. | filed (#60) |
| 28 | #40 | Documented limitations of the new drain, worth ratifying: it is not a snapshot (rows created/deleted/edited mid-drain may be missed or included); a row created mid-drain behind the passed boundary is skipped; and a caller filter already two levels deep cannot be `and`-wrapped, so the API will 400. | filed (#62) |
| 29 | #41 | **No replay protection on webhooks** — Notion sends no timestamp header, so a captured valid request can be replayed. Inherent to the scheme, not to our implementation, but docs should carry mitigation guidance (e.g. dedupe on event `id`). | filed (#62) |
| 30 | #41 | **Strictness choice to ratify:** uppercase-hex signatures are rejected. Notion sends lowercase and Node's reference `timingSafeEqual`-on-strings behaves the same, so this matches practice — but relaxing to case-insensitive is a one-line change if preferred. | filed (#62) |
| 31 | #41 | Webhook events are modelled as envelope + typed accessors, with `updated_properties` kept as raw JSON because it is genuinely polymorphic across event types (strings vs objects). A first-class sealed class per event type would enable exhaustive `when` at the cost of tolerance. Anyone remodelling **must** preserve that polymorphism. | wont-do — design constraint on a remodelling nobody has planned; preserved here and in the model's KDoc |
| 32 | #41 | `reference/notion-api/` has **no webhooks material at all**. Worth vendoring the live webhooks reference and the Event types & delivery page on the next reference refresh. Third corroboration of item 3. | filed (#60) |
| 33 | #42 | **Rollup schema config is the same gap, untouched:** `DatabaseProperty.Rollup.rollup` is still an untyped `JsonObject`, and rollups still cannot be created via the DSL. Natural sibling to #42. | done — `RollupConfiguration` + `RollupFunction` (24 functions, `UNKNOWN` fallback) replace the `JsonObject`, and `rollup(...)` DSL overloads make rollups creatable. Source-breaking; noted in CHANGELOG. |
| 34 | #42 | The readable-`prop()` syntax rollout is **gradual**, so a live read-side assertion must tolerate both the readable and the legacy `{{notion:block_property:...}}` syntax. `FormulaConfiguration.usesInternalReferences()` detects the legacy form. An integration test covering read and verbatim-write would be valuable. | filed (#61) |
| 35 | #42 | The vendored sample `reference/notion-api/sample_responses/databases/patch_update_a_database.json` uses `"value"` where the live API uses `"expression"` — a known old-docs sample bug. Ignored during #42; worth correcting on a reference refresh. | filed (#60) |
| 36 | #42 | `RequestValidator.validateDatabaseRequest` still only checks title/description; formula validation lives at construction time per the #31 precedent. If schema validation is ever consolidated into `RequestValidator`, these checks could move there. | done — decision: rollup validation follows the #31/#42 precedent and lives at construction time; `RequestValidator` was left untouched. No consolidation. |
| 37 | run-wide | **Three source-breaking changes landed on this branch** (#36 sealed-class additions, #39 `StatusConfiguration.options`, #42 `DatabaseProperty.Formula.formula`). All are captured in the batched CHANGELOG entry. Confirm the version bump reflects them before release. | filed (#62) |
| 38 | run-wide | **No integration test ran during this entire run** — deliberate, per the no-live-API rule. Several features are implemented at high confidence from live docs but never exercised against the real API: async tasks (#38), status groups (#39), the >10k drain (#40), formula writes (#42). A single integration pass before release would be well spent. | filed (#61) |

## Notes

- Issues #32–#42 are **not** auto-closed by their PRs: the PRs target this shared
  feature branch rather than `main`, so `Closes #NN` does not fire. They stay open
  until the shared branch lands on `main`.
