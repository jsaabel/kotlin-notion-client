# Follow-ups from the orchestrated issue run

Collected from implementing-agent reports on issues #32–#42, landed on the shared
feature branch `claude/kotlin-notion-orchestrate-9ztk5a`. Every entry is something
an agent **noticed and deliberately did not fix** because it fell outside the scope
of its issue. Nothing here has been independently verified — each item is recorded
as the agent reported it.

Status: `open` · `filed` (issue created) · `done` · `wont-do`

| # | From | Follow-up | Status |
|---|------|-----------|--------|
| 1 | #32 | `docs/error-handling.md` (~lines 300–345) documents a `RateLimitConfig` API that no longer exists (`strategy`, `baseDelayMs`, `maxDelayMs`, `respectRetryAfter`, and the CONSERVATIVE/BALANCED/AGGRESSIVE presets), and never states which statuses are retried. Already stale before this run. Resync it and document the 429/529/502/503/504 retry matrix. | open |
| 2 | #32 | No typed exception for service overload — a `529` that exhausts retries surfaces as a generic `NotionException.ApiError` with `status = 529`. A dedicated subtype, or one carrying `retryAfterSeconds`, would be an improvement. | open |
| 3 | #33 | Vendored API reference `reference/notion-api/` is behind the live changelog — nothing newer than `documentation/general/12_Upgrade_Guide_2026-03-11.md`. Agents are web-fetching the live changelog to confirm field shapes. Refreshing the vendored docs would remove that dependency. **Corroborated by #34** (`filter_properties` appears nowhere — not in `NotionAPI.yml`, not in the sample responses) **and #35** (zero mention of `unknown_block` anything; the whole truncation-metadata feature is absent). | open |
| 4 | #33 | `SearchFilter.property` still defaults to `"object"` in the primary constructor, so a hand-written `SearchFilter(inTrash = true)` that bypasses the builder emits a spurious `"property":"object"` alongside `in_trash`. Needs a decision: flip the default to `null` (behaviour change for direct constructor callers) or add `init` validation. | open |
| 5 | #33 | No `is_archived` convenience overload on `DataSourcesApi.query`/`queryAsFlow`/`queryFirstPage`, and no trash variant of `SearchApi.search(query: String)`. Both are reachable via the request/builder objects; left alone for minimal scope, but worth filing if the ergonomics bite. | open |
| 6 | #34 | IDEAS.md #5 is **not** closed by #34. The percent-decode fix lives in `PagesApi` only (`decodePropertyId`); normalising property IDs during `DataSource` schema deserialisation — what #5 actually asks for — is untouched. If #5 is picked up, consider promoting `decodePropertyId` into a shared util, which would make the `PagesApi` helper redundant. | open |
| 7 | #34 | `filter_properties` is documented as capped at 100 IDs; no validation was added, since `RequestValidator` is body-shaped and this is a query-param concern. Add fail-fast validation if wanted. | open |
| 8 | #34 | The same `filter_properties` parameter exists on data source query (`POST /v1/data_sources/{id}/query`), where the payload saving is larger still because we auto-paginate there. Out of scope for #34 — worth filing separately. | open |
| 9 | #35 | **CLAUDE.md documents build tasks that do not exist.** `./gradlew integrationTest` and `./gradlew testAll` are not configured — `build.gradle.kts` has only `tasks.test { useJUnitPlatform() }`, with no tag-based task split. Either add the tasks or fix the doc. Agents are working around it with `-Dkotest.tags.include="Unit"`. | open |
| 10 | #35 | The REST reference page for page-markdown (`developers.notion.com/reference/get-page-markdown`) 404s, and the Aug 7 2026 changelog frames truncation metadata as an MCP `notion-fetch` change. The REST shape of `unknown_block_count` is therefore inferred, not documented — worth confirming against a live response. | open |
| 11 | #35 | `CHANGELOG.md` has not been touched by any PR on this branch (#45–#48). Decision pending: batch one Unreleased → Added entry covering the whole run, or have each agent write its own. | open |
| 12 | #36 | **Same crash class still live:** Notion documents rollup value types as `array \| date \| incomplete \| number \| unsupported`. `RollupResult` has no `incomplete` variant, so `"type": "incomplete"` still throws and fails deserialization of the whole page. Needs its own issue. | open |
| 13 | #36 | **Forward-compat hardening:** `FormulaResult` and `RollupResult` have no catch-all fallback at all, unlike `PageProperty` with its `Unknown`. Any future nested type Notion adds hard-fails page deserialization. A `defaultDeserializer` on those two sealed classes would close the class of bug rather than one instance. | open |
| 14 | #36 | **Conflicting testimony on test scoping — verify.** The #36 agent reports plain `./gradlew test` has no tag filter in `build.gradle.kts` and would run integration specs (it scoped with `--tests "unit.*"`). Agents on #32/#33/#34/#35 ran plain `./gradlew test` and reported the `RequiresApi` specs skipped. **Settled by #37:** `build.gradle.kts` has no tag filtering, so a bare `./gradlew test` does load `integration.*` specs — they are guarded by their own `@Tags("Integration","RequiresApi")` and env-var checks, not by the build script. With `-Dkotest.tags.include="Unit"` they load but skip; with `--tests "unit.*"` they are not loaded at all. `NOTION_API_TOKEN` is unset in the agent environment, so no run reached the live API. Item 9 (the missing tasks) still stands. | done |
| 15 | #37 | **Pre-existing fixture/model mismatch:** `DataSource.description` is a required non-nullable `List<RichText>`, but the official sample `src/test/resources/api/data_sources/get_retrieve_a_data_source.json` has no `description` key — so `TestFixtures.DataSources.retrieveDataSource().decode<DataSource>()` throws `MissingFieldException`. No existing test decodes that fixture into `DataSource`, which is why it went unnoticed. Either default `description` to `emptyList()` or refresh the fixture. | open |
| 16 | #37 | If a mention-aware link resolver lands (deferred to v0.6.0+ per `RichTextHtml.kt` KDoc), that resolver is where a `notion.so` domain assumption could creep back in. Nothing depends on mention `href` shape today — `RichTextHtml` drops them. | open |
| 17 | #37 | `notebooks/README.md` and `QUICKSTART.md` link to `https://www.notion.so/my-integrations` — a settings page, not a record link, so out of scope for #37. Worth a sweep to `notion.com` eventually. | open |
| 18 | #38 | The Jun 29 2026 changelog also enables `allow_async` on `POST /v1/pages` when a `markdown` body is supplied. `PagesApi.create` has no markdown path at all, so that half of the feature has no surface in this client. Worth a follow-up if page-create-from-markdown lands. | open |
| 19 | #38 | No integration test covers the async markdown path: a 202 cannot be forced deterministically (the API decides), so a live test would be flaky. The `Completed` fallback makes the code safe either way, but an opportunistic integration exercise could be filed. | open |
| 20 | #38 | Async-task field optionality is modelled defensively (`operation`, `created_time` nullable) at **medium** confidence — endpoint path, five status values, `allow_async` and the 202 flow are high-confidence, cross-checked against Notion's MCP tool schema. Worth confirming minor-field optionality against a real 202 response. | open |
| 21 | #38 | `pollAsFlow` deliberately does not throw on a failed task (it completes at the first terminal status), unlike `waitForCompletion` which throws `AsyncTaskException.TaskFailed`. Intentional, but the asymmetry is worth a conscious ratification. | open |
| 22 | #39 | **IDEAS.md #4 is not unblocked**, contrary to the "bonus" note on #39. `NewFilterTypesIntegrationTest` also needs a `unique_id` property, still absent from the live property-schema reference's creatable types and from `CreateDatabaseProperty`. The status side of that setup is now fully API-doable. Either annotate #4 or file a narrower issue to make the test self-contained minus the unique_id filters. | open |
| 23 | #39 | Status group assignment is implemented at high confidence from live docs but **never exercised against the live API** — integration tests were out of bounds for this run. The existing "verify standard groups" test in `DatabaseFeaturesIntegrationTest` is the natural place for a live assertion. | open |
| 24 | #39 | Documented API behaviour worth ratifying: on update, an omitted `group` preserves the option's current group, and new options default to "To-do". Currently captured only in KDoc. | open |
| 25 | #40 | **`ViewsApi` has the same 10,000-row cap with no windowed drain.** `request_status` exists on `ViewQuery`/`ViewQueryResults`, but the new `iterateAllRows` engine covers data sources only. Likely deserves its own issue. | open |
| 26 | #40 | The windowing key choice is **the agent's design, not a documented one** — Notion's "Query large data sources" guide (linked from the changelog) 404s at every obvious URL. The assumption that a `unique_id` property supports property sorts is only fairly confident. A one-off live drain against a >10k-row data source would settle both. | open |
| 27 | #40 | The JS SDK named in #40 as prior art (`iterateAllDataSourceRows`/`collectAllDataSourceRows`) is **not vendored** under `reference/` — only `python/` and `notion-api/` are there. CLAUDE.md points agents at `reference/` implementations for cross-checking; worth vendoring the JS SDK or correcting that expectation. | open |
| 28 | #40 | Documented limitations of the new drain, worth ratifying: it is not a snapshot (rows created/deleted/edited mid-drain may be missed or included); a row created mid-drain behind the passed boundary is skipped; and a caller filter already two levels deep cannot be `and`-wrapped, so the API will 400. | open |
| 29 | #41 | **No replay protection on webhooks** — Notion sends no timestamp header, so a captured valid request can be replayed. Inherent to the scheme, not to our implementation, but docs should carry mitigation guidance (e.g. dedupe on event `id`). | open |
| 30 | #41 | **Strictness choice to ratify:** uppercase-hex signatures are rejected. Notion sends lowercase and Node's reference `timingSafeEqual`-on-strings behaves the same, so this matches practice — but relaxing to case-insensitive is a one-line change if preferred. | open |
| 31 | #41 | Webhook events are modelled as envelope + typed accessors, with `updated_properties` kept as raw JSON because it is genuinely polymorphic across event types (strings vs objects). A first-class sealed class per event type would enable exhaustive `when` at the cost of tolerance. Anyone remodelling **must** preserve that polymorphism. | open |
| 32 | #41 | `reference/notion-api/` has **no webhooks material at all**. Worth vendoring the live webhooks reference and the Event types & delivery page on the next reference refresh. Third corroboration of item 3. | open |

## Notes

- Issues #32–#42 are **not** auto-closed by their PRs: the PRs target this shared
  feature branch rather than `main`, so `Closes #NN` does not fire. They stay open
  until the shared branch lands on `main`.
