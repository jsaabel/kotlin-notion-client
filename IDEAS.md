# Ideas & Future Improvements

A running log of improvement ideas captured during development. These are asides that don't belong to any specific task but are worth revisiting.

Use `/capture-idea <your idea>` to add entries. Use `/implement-idea <row number>` to mark one as implemented.

**Status values:** `open` · `in progress` · `done` · `wont-do`

---

| # | Date | Idea | Status |
|---|------|------|--------|
| 1 | 2026-04-06 | Integration tests should print out links to created pages, to make verification more convenient | done   |
| 2 | 2026-04-06 | Reconsider the `PageIcon` sealed class name — icons are used across pages, databases, callouts, tabs, and (soon) native icons. A more neutral name like `NotionIcon` or `BlockIcon` might better reflect the concept's scope. Worth evaluating before the 0.4.0 release since it's a breaking rename. | done  |
| 3 | 2026-04-06 | the integration tests should be consolidated at some point. right now we have too many doing very dedicated things, which has its merits, but for verification it would be more convenient to reduce on the number of tests, combining functionality rather than removing tested features | done   |
| 4 | 2026-04-09 | `NewFilterTypesIntegrationTest` relies on manual setup (persistent `NOTION_TEST_DATASOURCE_ID`) because `status` and `unique_id` properties couldn't be created via API when it was written. Once Phase 6 adds status property creation to `DatabaseRequestBuilder`, this test should be refactored to be self-contained (create database → populate → assert → cleanup) like `DataSourcesIntegrationTest`. **Still blocked (re-triaged 2026-09-03, post-0.6.0): `status` is now API-creatable, but `unique_id` remains absent from the property-schema reference's creatable types and from `CreateDatabaseProperty`, so full self-containment is impossible — this is a Notion API limitation, not ours, and unblocks only if Notion makes `unique_id` creatable. Actionable now without waiting: split the spec so only the unique_id filter tests need the persistent `NOTION_TEST_DATASOURCE_ID`, and let the rest become self-contained.** | open   |
| 5 | 2026-04-13 | Normalize property IDs from DataSource schema: the API returns them percent-encoded (e.g. `ue%5Cl`) in the data source schema but decoded (e.g. `ue\l`) in view filter/sort responses. Consider URL-decoding IDs transparently during deserialization of DataSource properties so callers always get consistent IDs. Done 2026-08-22: `DatabasePropertySerializer` now decodes `id` before dispatching to each `DatabaseProperty` variant; the decode-with-fallback helper was promoted from `PagesApi`-local to a shared `it.saabel.kotlinnotionclient.utils.PropertyIds.decode` util. | done   |
| 6 | 2026-04-13 | Database icons set via `databases.create { icon.emoji(...) }` were not persisting in the UI because Notion renders the *data source* view (not the container). Fixed 2026-04-14: `DatabasesApi.create()` now auto-propagates the icon to the initial data source via a follow-up PATCH to `/data_sources/{id}` immediately after creation. | done   |
| 7 | 2026-04-14 | Kotlin Notebooks seem to work again (tested with 0.3.0). Update them to use 0.4.0, make any adjustments that new 0.3/0.4 functionality requires. Consider dedicated notebooks introducing 0.3.0 and 0.4.0 features respectively, and reference them in the README/release notes. Add a note on which IntelliJ IDEA version is currently in use. **Re-triaged 2026-09-03: never actioned — the notebooks still pin 0.4.1. Now tracked properly as [#87](https://github.com/jsaabel/kotlin-notion-client/issues/87), which supersedes this row and widens it to a 0.6.0 refresh (re-run against a live workspace, commit real output cells, fix the stale `docs/notebooks.md` claims). Track it there, not here.** | open |
| 8 | 2026-04-14 | Timezone functionality may need a proper test and adjustments — see example in `05-rich-text-dsl.ipynb`. Fixed 2026-04-14: `dateMention(LocalDateTime, TimeZone)` was converting to a UTC instant before sending, causing Notion to misinterpret the time. Fixed to pass the local datetime string + `timezone.id` directly. Notion never preserves named timezones in responses (always returns `time_zone=null` with a numeric offset like `-04:00`). Verified via `TimezoneIntegrationTest`. | done |
| 9 | 2026-04-14 | Comprehensive analysis of and update plan for README/documentation before 0.4.0 release. Done 2026-09-03 (re-triage): overtaken by events well past its 0.4.0 target — `docs/` now carries 20 topic guides plus `dsl-conventions.md` and three ADRs, 26 commits touched `README.md`/`docs/` between v0.4.0 and v0.6.0, and documented snippets are now compile-checked by the eight `src/test/kotlin/examples/*Examples.kt` files. Remaining doc debt is specific rather than systemic and is tracked as issues (#87 for the notebooks). | done |
| 10 | 2026-04-15 | Verify/improve limited-result querying: `pageSize(N)` currently auto-paginates and always returns ALL results. The Notion API supports returning only the first page of N results without fetching further pages. We should check whether callers can opt out of auto-pagination when they genuinely only want the first N results (e.g. a "top 5" query). May need a separate `query` overload or a `limit(N)` option that stops after one API call. Notebook 06 Example 11 currently misrepresents this as a feature rather than a limitation. Done 2026-09-03 (re-triage): `DataSourcesApi.queryFirstPage(dataSourceId, filterProperties?, builder)` makes exactly one API call and returns the raw `DataSourceQueryResponse` with `hasMore` and `nextCursor`, so `pageSize(5)` genuinely means five — its KDoc carries the "top 5 dashboard query" example verbatim. An `isArchived` overload mirrors it. The notebook-06 correction is folded into [#87](https://github.com/jsaabel/kotlin-notion-client/issues/87). | done |
| 11 | 2026-08-22 | Does `PATCH /v1/data_sources` accept a `cover`? Answered 2026-08-22: **no** — the endpoint replies `The `cover` property is not supported for data sources. Use the Update Database API instead.` So the missing cover builder on `UpdateDataSourceRequestBuilder` is the API's asymmetry, not an oversight, and the comment there now cites the message. The same run settled three siblings: a database container `icon` and `cover` can be replaced but not cleared (`should be an object or `undefined``), while a data source `icon` *can* be cleared — so `dataSources.update { icon.remove() }` was added, and it is the removal that matters since the UI renders the data source. Matrix pinned by `IconCoverSupportIntegrationTest`. Surfaced by #82. | done |
